# Development Caddyfile - HTTP only, localhost
{
    auto_https off
    admin off
}

:8000 {
    handle /api/* {
        reverse_proxy backend:8000
    }
    
    handle {
        reverse_proxy frontend:3000
    }
    
    log {
        output stdout
        level DEBUG
    }
}
